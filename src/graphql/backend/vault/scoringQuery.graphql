query Scoring($vaultAddress: String!) {
  vaults(id: $vaultAddress) {
    scoring {
      consensusRewardsEarned,
      consensusRewardsMissed,
      executionMevEarned,
      executionMevMissed
    }
  }
}
