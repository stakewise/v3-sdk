query Validators(
  $vaultAddress: String!
  $skip: Int!
  $first: Int!
) {
  vaultValidators(
    vaultAddress: $vaultAddress,
    skip: $skip,
    first: $first,
  ) {
    apy: apr
    earned
    createdAt
    publicKey
  }
}
