query Allocators($address: Bytes!, $vaultAddress: String!) {
  allocators(
    where: {
      address: $address,
      vault: $vaultAddress
    }
  ) {
    assets
    totalEarnedAssets
    totalOsTokenFeeAssets
    totalStakeEarnedAssets
    totalBoostEarnedAssets
    totalExtraEarnedAssets
  }
}
