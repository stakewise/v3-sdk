query VaultStats($vaultAddress: String!, $limit: Int) {
  vaultStats: vaultSnapshots(
    first: $limit
    orderBy: timestamp
    orderDirection: desc
    where: {
      vault: $vaultAddress
    }
  ) {
    apy
    timestamp
    totalAssets
    earnedAssets
  }
}
