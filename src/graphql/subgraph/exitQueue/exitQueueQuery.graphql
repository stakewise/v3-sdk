query exitQueue($where: ExitRequest_filter) {
  exitRequests(where: $where) {
    isClaimed
    timestamp
    totalAssets
    isClaimable
    exitedAssets
    positionTicket
    exitQueueIndex
    withdrawalTimestamp
  }
}
