query exitQueue($receiver: Bytes, $vault: String!) {
  exitRequests(where: {
    receiver: $receiver,
    vault: $vault,
    isClaimed: false
  }) {
    timestamp
    totalAssets
    isClaimable
    isV2Position
    exitedAssets
    positionTicket
    exitQueueIndex
    withdrawalTimestamp
  }
}
