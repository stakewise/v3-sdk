query exitQueue($where: ExitRequest_filter) {
  exitRequests(where: $where) {
    timestamp
    totalAssets
    isClaimable
    isV2Position
    exitedAssets
    positionTicket
    exitQueueIndex
    withdrawalTimestamp
  }
}
