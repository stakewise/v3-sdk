query exitQueue($where: ExitRequest_filter) {
  exitRequests(where: $where, first: 1000) {
    receiver
    isClaimed
    timestamp
    totalAssets
    isClaimable
    exitedAssets
    isV2Position
    positionTicket
    exitQueueIndex
    withdrawalTimestamp
  }
}
