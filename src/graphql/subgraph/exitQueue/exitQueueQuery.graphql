query exitQueue($where: ExitRequest_filter) {
  exitRequests(where: $where) {
    timestamp
    totalAssets
    isClaimable
    exitedAssets
    positionTicket
    exitQueueIndex
    withdrawalTimestamp
  }
}
