query exitQueue($receiver: Bytes, $vault: String!) {
  exitRequests(where: {
    receiver: $receiver,
    vault: $vault,
  }) {
    withdrawalTimestamp
    positionTicket
    isV2Position
    totalAssets
    timestamp
    exitQueueIndex
    exitedAssets
    isClaimable
  }
}
