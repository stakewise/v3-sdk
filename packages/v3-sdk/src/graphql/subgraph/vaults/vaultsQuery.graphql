query Vaults(
  $skip: Int,
  $first: Int,
  $where: Vault_filter,
  $orderBy: Vault_orderBy,
) {
  vaults(
    skip: $skip,
    first: $first,
    where: $where,
    orderBy: $orderBy
    orderDirection: desc,
  ) {
    ...VaultShort
  }
}
