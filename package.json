{
  "name": "v3-sdk",
  "version": "0.1.0",
  "description": "StakeWise V3 SDK",
  "homepage": ".",
  "private": true,
  "scripts": {
    "prepare": "npm i lerna && lerna bootstrap",
    "todo_prepare": "npm i lerna && lerna bootstrap && lerna run generate",
    "build": "npm run build --prefix=./packages/stakewise-dev",
    "test": "lerna run test",
    "test-coverage": "lerna run test-coverage"
  },
  "license": "AGPL-3.0-only",
  "devDependencies": {
    "@babel/preset-env": "7.22.4",
    "@babel/preset-typescript": "7.21.5",
    "@types/jest": "29.5.1",
    "husky": "8.0.3",
    "jest": "29.5.0",
    "jest-cli": "29.5.0",
    "ts-jest": "29.1.0"
  },
  "dependencies": {
    "lerna": "6.6.2"
  }
}
