name: Sync docs to StakeWise Docs

on:
  pull_request:
    branches: [ main ]
    paths:
      - 'src/**/*.md'
      - 'src/**/*.mdx'

permissions:
  contents: write
  pull-requests: read

jobs:
  sync-docs:
    if: github.event_name == 'push' || github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Enable SSH commit signing
        env:
          SYNC_DOCS_SSH_KEY: ${{ secrets.SYNC_DOCS_SSH_KEY }}
        run: |
          mkdir -p ~/.ssh
          echo "$ACTIONS_SSH_SIGNING_KEY" > ~/.ssh/signing_key
          chmod 600 ~/.ssh/signing_key

          git config --global gpg.format ssh
          git config --global user.signingkey ~/.ssh/signing_key
          git config --global commit.gpgSign true
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Configure SSH for pushing
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          ssh-keyscan github.com >> ~/.ssh/known_hosts
          chmod 644 ~/.ssh/known_hosts

      - name: Install sync tools
        run: npm ci

      - name: Run docs sync
        run: npm run sync:docs
